#include <stdio.h>

int main()
{
    int number;
    int factorial = 1;
    
    printf("Enter value to find factorial: ");
    scanf("%d", &number);
    
    while(number>0){
        factorial = factorial * number;
        number--;
    }
    
    printf("factorial is %d", factorial);

    return 0;
}
